{
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.node.tsbuildinfo",
    "target": "es2023",
    /* "DOM" is for the previewManifest plugin, which references the `Document`
       type when parsing catalog XML with @xmldom/xmldom. No runtime impact. */
    "lib": ["ES2023", "DOM"],
    "module": "esnext",
    "types": ["node"],
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noEmit": true,

    /* Linting */
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "erasableSyntaxOnly": true,
    "noFallthroughCasesInSwitch": true
  },
  /* src/vite-env.d.ts is listed explicitly (it is imported by nothing): the
     previewManifest plugin pulls src/ksa/catalog.ts into this program, whose
     assetBase() references `import.meta.env`. */
  "include": ["vite.config.ts", "vite/**/*.ts", "apps/*/vite.config.ts", "src/vite-env.d.ts"]
}
